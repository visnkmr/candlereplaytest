/** @type {import('next').NextConfig} */
const nextConfig = {
  // Temporarily disable static export to test build
  // output: 'export',
  // images: {
  //   unoptimized: true
  // },
  // trailingSlash: true,
  // distDir: 'out'
};

module.exports = nextConfig;

//updated dec25
